# Local-only. Copy to keystore/keystore.properties (gitignored).
# Never commit that file, never put it in a shared zip.
storeFile=../keystore/mobile-market-upload.jks
storePassword=
keyAlias=upload
keyPassword=
