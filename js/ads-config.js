/* AdSense — website / Android TWA only (never a WebView wrapper).
   After Google AdSense approval, set enabled to true and paste your ca-pub- id. */
window.MW_ADS = {
  enabled: false,
  client: "",
  slots: {
    home: "",
    list: ""
  }
};
